// ===================================== Array =====================================

/*
let arrayBidimensional = [
  [firstArray],
  [secondArray],
];
*/

/*
console.log(arrayName[0][1]); // accede a un elemento en un array dentro de otro array
*/

/*
array.push(element); // agrega un elemento al final del array
let ultimaEstacion = estaciones.pop(); // retorna y elimina el ultimo elemento de un array
let firstElement = array.shift(); // retorna y elimina el primer elemento de un array
array.unshift(element); // agrega un elemento al principio de un array
*/

/*
console.log(JSON.stringify(array)); // muestra un array de forma mas correcta
*/

// =================================== Functions ===================================

/*
function functionName(firstParameter, secondParameter) {
  // content of function
}
*/
